# Sponsor assets

Real-world sponsor logos go here (SVG preferred, PNG fallback). Reference them
from templates as `/assets/sponsors/<file>` — everything in `src/assets/` is
copied to the built site as-is. This is the one folder where non-generated
imagery is allowed.
